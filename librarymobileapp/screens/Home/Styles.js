import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 10
    },
    search: {
        marginBottom: 10
    },
    card: {
        marginBottom: 10
    }
});